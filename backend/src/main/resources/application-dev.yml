spring:
  # H2 개발 환경
  datasource:
    url: jdbc:h2:mem:oneonone_dev
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

# 로깅
logging:
  level:
    com.oneonone: DEBUG
